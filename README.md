# fdtrace

## Prerequisites

TODO

## Run

```
> cd fdtrace
> sudo -E cargo rr --help
> sudo -E cargo rr --debug (whereis ls)
```

To run the example:
```
> cargo br
> sudo -E cargo rr target/release/example
```
